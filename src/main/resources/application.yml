server:
  port: 8080
  servlet:
    context-path: /bootcamp

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/santander-dev-week
    username: root
    password: admin
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: none
